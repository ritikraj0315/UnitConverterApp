<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConverterApp"
        tools:targetApi="31">
        <activity
            android:name=".activities.VolumeActivity"
            android:exported="false"
            android:theme="@style/Theme.ConverterApp.NoActionBar" />
        <activity
            android:name=".activities.MassActivity"
            android:exported="false"
            android:theme="@style/Theme.ConverterApp.NoActionBar" />
        <activity
            android:name=".activities.LengthActivity"
            android:exported="false"
            android:theme="@style/Theme.ConverterApp.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ConverterApp.NoActionBar"
            android:windowSoftInputMode="stateHidden"
            tools:ignore="LockedOrientationActivity"
            tools:node="merge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>